{
  "name": "tweet-milestone",
  "type": "module",
  "scripts": {
    "zip": "zip -r TweetMilestone.zip .",
    "deploy": "aws lambda update-function-code --function-name TweetMilestone --zip-file fileb://TweetMilestone.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.259.0",
    "@aws-sdk/util-dynamodb": "^3.267.0",
    "dotenv": "^16.0.3"
  }
}
